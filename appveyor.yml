version: "{build}"

environment:
  GOPATH: c:\gopath

platform:
  - x86
  - x64

branches:
  only:
    - master

clone_folder: c:\gopath\src\github.com\richardlehane\siegfried

build_script:
  - go install -a github.com/richardlehane/siegfried/cmd/sf
  - go install -a github.com/richardlehane/siegfried/cmd/roy

artifacts:
  - path: c:\gopath\bin
    name: siegfried-$(PLATFORM)

deploy:
  - provider: GitHub         
    draft: false
    prerelease: false
    on:
      platform: x86                